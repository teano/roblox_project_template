PROOFREADER_ID: tf0010-ui-spec-proofreader-r3-04
PRD_SHA256: 31f00b75820a2a917ea55c1f486a3846b7e273eb689f563601ec2093269c0995
SPEC_SHA256: cfb6012d2232faeea1feaf997d7dfc8ab84317d57162c6c5bdbc971a9da3068d
COVERAGE_COMPLETE: yes

FINDINGS:
F-R3-04-001 | Major | public_contract | PRD-REQ-039, PRD-REQ-105, PRD-REQ-106, PRD-AC-009, PRD-AC-030 | Spec §4.5 permits default nomination only on a `0 -> nonzero` eligible transition and fails removal of the current default whenever another eligible member remains; §4.6 exposes only `RemoveNestedUiElement(child)` and explicitly provides no reconfiguration API. Therefore a dynamically attached subtree that owns the current default can become impossible to remove while a static or independently owned eligible member remains, so the promised dynamic removal contract has no successful public operation that preserves the required valid default. | Add one failure-atomic removal overload accepting a nominated replacement default only when removal of the current default leaves eligible members; validate the nominee in the staged post-state and commit default replacement and detach atomically, with success and invalid/missing-nominee rollback coverage. Keep the general no-reconfigure and no-fallback rules.
F-R3-04-002 | Major | boundary | PRD-REQ-099, PRD-REQ-100, PRD-AC-028 | Spec §4.3 assigns identical runtime absence of the exact derived path two incompatible meanings: an empty source in the reusable template, but bootstrap failure in an initialized derived repository. The specified runtime inputs contain no source-authorized template/derived discriminator, and the existing repository-initialization marker is documentation outside the DataModel; exact-path resolution therefore cannot implement that distinction without an unspecified detector or heuristic. | Remove the bootstrap fail-closed assertion for total path absence and make mandatory project initialization plus repository validation the enforcement boundary already required by the PRD. If runtime distinction remains required, explicitly define and own a source-authorized composition marker instead of inferring repository kind from names, cloud IDs, or folder accidents.
F-R3-04-003 | Major | scope | PRD-REQ-100, PRD-AC-028 | PRD explicitly requires a derived-project initialization rule, skill, and documentation to require and verify `DerivedWindowConfig`. Spec §4.7 names the rule, documentation behavior, validator, and window-authoring skill, but only says `targeted project-initialization artifacts`; its exact implementation-cascade list omits any derived-project initialization skill, and the current repository contains no such repo-local skill to receive the requirement. TS-REQ-014 therefore does not provide an implementable artifact target for the required skill. | Name and include the exact repo-local derived-project initialization skill artifact (creating it if absent), and require it to create and validate the canonical `DerivedWindowConfig` path consistently with the initialization rule, documentation, and repository validator.

UNRESOLVED_COUNTS: product=0, scope=0, boundary=0, ownership=0, public_contract=0
QUESTION_IDS: none
MINORS_ENGINEER_RESOLVABLE: yes
COUNTS: critical=0, major=3, minor=0, total=3; PRD-REQ=175/175, PRD-NFR=6/6, PRD-AC=91/91; missing=0, unknown=0
VERDICT: revise
