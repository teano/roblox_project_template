# Compatibility pointer: project initialization

This filename is retained for one migration release so older derived projects
and historical documents do not fail on a missing link.

Current authority is `.agents/rules/template-workflow.md`. Use the
repository-owned `scripts/template-project.ps1 init` command. Do not execute
the retired mandatory ADR, feature-dashboard, CodeGraph, pipeline, shared-port,
or pre-edit Rojo workflow from older revisions.
