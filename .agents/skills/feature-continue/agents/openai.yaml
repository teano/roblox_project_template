interface:
  display_name: "Continue Feature"
  short_description: "Run a user-authorized feature Continue"
  default_prompt: "Use $feature-continue only after I explicitly ask to resume a paused repository-owned feature."

policy:
  allow_implicit_invocation: false
