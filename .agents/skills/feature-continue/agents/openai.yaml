interface:
  display_name: "Continue Feature"
  short_description: "Resume an open feature in the same turn"
  default_prompt: "Use $feature-continue to recover the open feature context and continue its concrete next step now."

policy:
  allow_implicit_invocation: true
