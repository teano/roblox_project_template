interface:
  display_name: "Continue Feature"
  short_description: "Restore context and continue feature work"
  default_prompt: "Use $feature-continue to restore context and continue an in-progress repository-owned feature."

policy:
  allow_implicit_invocation: false
