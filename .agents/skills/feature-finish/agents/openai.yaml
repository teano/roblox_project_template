interface:
  display_name: "Finish Feature"
  short_description: "Run a user-authorized feature Finish"
  default_prompt: "Use $feature-finish only after I explicitly ask to document and close completed feature work."

policy:
  allow_implicit_invocation: false
