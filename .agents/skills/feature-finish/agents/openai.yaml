interface:
  display_name: "Finish Feature"
  short_description: "Verify and close a completed feature"
  default_prompt: "Use $feature-finish to complete the remaining scoped work, verify it, and close the feature record when ready."

policy:
  allow_implicit_invocation: true
