interface:
  display_name: "Pause Feature"
  short_description: "Checkpoint open work without changing state"
  default_prompt: "Use $feature-pause to save a concise handoff for the open feature and stop work for now."

policy:
  allow_implicit_invocation: true
