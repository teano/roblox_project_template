interface:
  display_name: "Pause Feature"
  short_description: "Run a user-authorized feature Pause"
  default_prompt: "Use $feature-pause only after I explicitly ask to checkpoint and pause the active repository-owned feature."

policy:
  allow_implicit_invocation: false
