interface:
  display_name: "Start Feature"
  short_description: "Run a user-authorized Start or Reopen"
  default_prompt: "Use $feature-start only after I explicitly ask to start or reopen a named repository-owned feature."

policy:
  allow_implicit_invocation: false
