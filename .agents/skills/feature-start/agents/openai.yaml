interface:
  display_name: "Start Feature"
  short_description: "Create an open feature and begin work"
  default_prompt: "Use $feature-start to create a lightweight feature record and begin the requested work in the same turn."

policy:
  allow_implicit_invocation: true
